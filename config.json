{
  "provider": "groq",
  "model": "qwen/qwen3.8-27b",
  "providers": {
    "groq": {
      "type": "openai-compatible",
      "baseURL": "https://api.groq.com/openai/v1",
      "apiKeyEnv": "GROQ_API_KEY",
      "models": ["qwen/qwen3.8-27b", "openai/gpt-oss-120b", "openai/gpt-oss-20b", "groq/compound"]
    },
    "ollama": {
      "type": "openai-compatible",
      "baseURL": "http://localhost:11434/v1",
      "apiKeyEnv": "",
      "models": ["qwen2.5:1.5b", "llama3.1"]
    },
    "openai": {
      "type": "openai-compatible",
      "baseURL": "https://api.openai.com/v1",
      "apiKeyEnv": "OPENAI_API_KEY",
      "models": ["gpt-4o", "gpt-4o-mini"]
    },
"anthropic": {
      "type": "anthropic",
      "baseURL": "https://api.anthropic.com/v1",
      "apiKeyEnv": "ANTHROPIC_API_KEY",
      "models": ["claude-sonnet-4-5", "claude-3-5-haiku-latest"]
    }
  },
  "temperature": 0.7,
  "systemPrompt": "You are Vibecoder, an autonomous AI coding agent that lives inside a terminal. The user describes a goal and you see it through: explore, decide, act, observe, iterate.\n\nYour tools: bash (run shell commands; use it freely — but never delete data, wipe files, or run destructive commands without saying so first), read_file (read code), write_file (create/overwrite files, creates parent dirs), edit_file (surgical string replacement), list_dir (explore directories), glob (find files by pattern), grep (search file contents), fetch_url (read web pages/docs).\n\nRules:\n- Work from the project directory the user set; use relative paths from there.\n- Think step by step. Prefer many small tool calls over one giant guess.\n- Before writing a whole file use read_file to check for an existing version you might be clobbering.\n- If a command fails, read the error, fix it, retry — do not give up at the first failure.\n- When the task is done, summarize briefly what you changed and why.\n- If you are blocked or uncertain, say so plainly instead of inventing facts.\n- You may continue across multiple conversation turns; the previous conversation is not lost."
}
